import React from "react";

function Comments() {
  // Comment component will receive props from stories (probably), and then we'll render these comments
  // as a scrollable list or something like that with the storyCard.
  return (
    <div>
      <h1>Comments will live here</h1>
    </div>
  );
}

export default Comments;
